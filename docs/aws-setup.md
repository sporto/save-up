AWS Setup
=========

Install aws cli

Configure CLI
-------------

Generate IAM access keys from AWS console. Must have permissions to:

- AmazonS3FullAccess

```
aws configure --profile=something
```
